spring.application.name=examlms

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://Kaleidoscope:453145@kaleidoscope.az65wnr.mongodb.net/examlms?retryWrites=true&w=majority&appName=kaleidoscope
spring.data.mongodb.database=examlms

# MongoDB Connection Pool
spring.data.mongodb.auto-index-creation=true

# Server Configuration
server.port=8080

# JWT Configuration (if not already configured)
# jwt.secret=your-secret-key-here
# jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.kaleidoscope.j2ee.examlms=DEBUG
